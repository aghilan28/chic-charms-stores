/* ============================================================
   auth-nav.js - Chic Charms
   Injects Login / My Account / Logout into #navActions
   on every page that loads this script.
============================================================ */

import { setupAuthUI } from "./auth-ui.js";
setupAuthUI();
